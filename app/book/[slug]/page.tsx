"use client";
import { useState } from "react";

export default function BookingPage(){const [result,setResult]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch("/api/bookings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const payload=await response.json();setResult(response.ok?`Booking created: ${payload.booking.id}. Pay the testnet XRP amount before the appointment.`:payload.error||"Could not create booking. Please sign in first.");}return <main className="shell"><nav className="nav"><a className="brand" href="/">proof<i>ly</i></a></nav><section className="details"><h1 style={{fontSize:48}}>Book David</h1><p className="lead">Mathematics Tutoring · €25/hour</p><form className="profile" onSubmit={submit}><label>Date<br/><input name="scheduled_for" type="datetime-local" required style={{margin:"8px 0 16px",padding:10,width:"100%"}}/></label><label>Hours<br/><input name="hours" type="number" min="1" max="8" defaultValue="2" required style={{margin:"8px 0 16px",padding:10,width:"100%"}}/></label><button className="button">Confirm booking</button>{result&&<p className="notice">{result}</p>}</form></section></main>}
